# MatCalc Documentation

## Install prerequisites

`pip install -r requirements.txt`

## Serve locally for development

For automatic reload of website on changes.

`mkdocs serve`

## Build static website

`mkdocs build`